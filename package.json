{
     "scripts": {
        "build-front": "(pushd frontend && npm run build && rm -rf ../backend/build && cp -r build ../backend/build && popd)",
        "start": "(pushd backend && npm install && npm start)"
    }
}