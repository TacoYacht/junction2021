{
     "scripts": {
          "build-front": "(cd frontend && npm run build && rm -rf ../backend/build && cp -r build ../backend/build && cd ..)",
          "start": "(cd backend && npm install && npm start)"
     }
}
